interface:
  display_name: "WorldJen — Bench"
  short_description: "Benchmark/evaluate an entire AI model (only when the target is a model). Create, inspect, compare Bench runs (worldjen bench create / get / list)."

policy:
  allow_implicit_invocation: true
