interface:
  display_name: "WorldJen — Rank"
  short_description: "Rank/compare several clips that share a prompt into a leaderboard. Only when rankings are explicitly asked for (worldjen rank upload)."

policy:
  allow_implicit_invocation: true
