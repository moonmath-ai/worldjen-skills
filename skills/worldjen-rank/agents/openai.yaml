interface:
  display_name: "WorldJen — Rank"
  short_description: "Rank multiple clips that share a prompt into a leaderboard; session locks to the first prompt (worldjen rank upload)."

policy:
  allow_implicit_invocation: true
