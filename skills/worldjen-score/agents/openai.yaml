interface:
  display_name: "WorldJen — Score"
  short_description: "Score a single generated clip. Raw per-dimension scores with optional custom prompt and dimensions (worldjen score upload)."

policy:
  allow_implicit_invocation: true
