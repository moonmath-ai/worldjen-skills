interface:
  display_name: "WorldJen — Score"
  short_description: "Score an individual clip / video / image (NOT a whole model). Default for 'score it' on media; raw per-dimension scores (worldjen score upload)."

policy:
  allow_implicit_invocation: true
